{
   "_id": "_design/game_saves",
   "views": {
       "player_game": {
           "map": "function(doc) { if (doc.type == 'save')  emit([doc.player_uuid], {game_uuid:doc.game_uuid, save_uuid:doc._id, save_name:doc.save_name, date:doc.date}) }"
       },
       "player_save": {
           "map": "function(doc) { if (doc.type == 'save')  emit([doc.player_uuid, doc._id], doc) }"
       }
   }
}